-- local env = require(game:GetService("ReplicatedStorage").src.env)
-- local genes = env.src.genes
-- local tableau = require(env.packages.axis.lib.tableau)

return {
	instanceTag = "gene__template",
	name = "_template",
	genes = {},
	state = {
		_template = {
		},
	},

	config = {
		_template = {
		},
	},
}
